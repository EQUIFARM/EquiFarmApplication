<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/> <!-- Background color -->
            <corners android:radius="8dp"/>  <!-- Rounded corners -->
        </shape>
    </item>

    <!-- Border with shadow effect -->
    <item android:left="0dp" android:right="0dp" android:top="0dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="#CCCCCC"/> <!-- Border color -->
            <corners android:radius="8dp"/>
            <stroke
                android:width="2dp"
                android:color="#888888"/> <!-- Border stroke width and color -->
        </shape>
    </item>

</layer-list>
